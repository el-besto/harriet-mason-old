<!-- views/album.ejs taken from node-gallery module -->
<% include ./layouts/header2 %>


<div class="row">
  <div class="small-12 columns">
    
    <h1>GALLERY</h1>
    <p>
      Below are some pictures from various times in Harriet’s life. Feel free to comment on any of the pictures, or contact Aurelia to add additional pictures.
    </p>
    <p>
      <a href="mailto:aurelia@powerfullatinas">(Aurelia@powerfullatinas.com</a>
    </p>
  
  </div>
</div>


<!-- gallery posts -->
<div class="row">
  <div class="small-12 columns">
    
    <% if (!album.isRoot){ %>
      <% for (var i=0; i<breadcrumb.length; i++) { %>
      <% if (i==breadcrumb.length-1){ %>
      <%= breadcrumb[i].name %>
      <% }else{ %>
      <a href="<%= breadcrumb[i].url %>"> <%= breadcrumb[i].name %> &raquo; </a>
      <% } %>

      <% } %>
    <% } %>
    
    <h3><%= album.prettyName %></h3>
      <% if (album.description){ %>
        <p><%- album.description %></p>
      <% } %>

      <% if (album.albums.length>0){ %>
      <h3>Albums</h3>
      <ul class="albums">
        <% album.albums.forEach(function(album){ %>
          <li class="thumb">
            <a href="<%= rootDir%>/<%= album.path%>">
              <div class="image"><img src="/<%= directory %>/<%= album.thumb %>&tn=1" alt="<%= album.prettyName %>"><br /></div>
              <span class="caption"><%= album.prettyName %></span>
            </a>
          </li>
        <% }) %>
      </ul>
    <% } %>
    
    <% if (album.photos.length>0){ %>
      <h2>Photos</h2>
      <ul class="photos">
        <% album.photos.forEach(function(photo){ %>
          <li class="thumb">
            <a href="<%=rootDir %><%= photo.path %>">
              <img src="/<%= directory %>/<%= photo.path %>&tn=1" alt="<%= photo.name%>"><br />
              <%= photo.name%>
            </a>
          </li>
        <% }) %>
      </ul>
    <% } %>
  
  </div>
</div>

<% include ./layouts/footer %>